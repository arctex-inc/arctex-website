import React from 'react';
import Home from './Home';

const TextboxContent = () => {
  return (
    // if first time loading in then display home
    <Home />
    //if clicked on formhelper 3d object, display formhelper content on textbox.
  );
};

export default TextboxContent;